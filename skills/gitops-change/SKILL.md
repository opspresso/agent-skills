---
name: gitops-change
description: >
  클러스터에 떠 있는 것을 바꿔야 할 때 로드해요. 리소스·설정·복제 수·이미지 버전
  변경을 클러스터가 아니라 GitOps 저장소의 chart values 변경으로 옮기고, 어느
  파일을 고쳐 어떻게 PR을 올릴지 정해요.
---

# GitOps 변경

**클러스터를 직접 바꾸지 않아요.** 변경은 저장소를 고치는 일이고, 반영은 Argo CD가
해요.

이유는 두 가지예요. Argo Application이 `selfHeal: true`, `prune: true`로 돌고 있어서
클러스터에 직접 넣은 변경은 곧 되돌려져요. 그리고 kubernetes 도구는 읽기 전용이라
애초에 바꿀 수단이 없어요. `kubectl apply`나 `edit`를 제안하는 건 둘 중 어느 쪽으로도
답이 아니에요.

## 반드시 지킬 것

- 변경 요청을 받으면 **어느 파일의 어느 값을 바꿀지**로 번역한 뒤 시작해요.
- 저장소 구조를 짐작하지 않아요. 해당 저장소의 README와 실제 `charts/` 아래를
  확인하고 나서 파일을 고르세요. 아래 규칙은 현재 `argocd-env-demo` 기준이에요.

## 어느 파일을 고치나

```
charts/<project>/
  values.yaml                  # 모든 phase 공통
  values-<phase>.yaml          # 특정 phase. 배포 파이프라인이 갱신하는 파일
  <env>/values-<cluster>.yaml  # 특정 클러스터
apps/<name>.yaml               # ApplicationSet (새 앱을 추가할 때)
```

| 바꾸려는 것 | 고칠 파일 |
|---|---|
| 모든 환경에 적용될 설정 | `charts/<project>/values.yaml` |
| 한 phase에서만 다른 값 | `charts/<project>/values-<phase>.yaml` |
| 한 클러스터에서만 다른 값 | `charts/<project>/<env>/values-<cluster>.yaml` |
| 새 앱 추가 | `apps/<name>.yaml` + `charts/<name>/` |

아래에서 덮어쓰므로, **가장 좁은 범위의 파일에 넣어요.** 한 클러스터만 고치면
되는 값을 `values.yaml`에 넣으면 전 환경이 따라 움직여요.

## 이미지 버전은 손으로 고치지 않아요

배포 파이프라인이 `repository_dispatch`로 `values-<phase>.yaml`을 갱신해요. 같은
파일을 사람이 동시에 고치면 충돌하거나 다음 배포에 덮여요.

버전을 올려 달라는 요청은 파일 수정이 아니라 **배포를 트리거하는 일**이에요. 직접
값을 바꾸기 전에 그게 맞는지 확인해요.

## PR

`prod`는 PR을 거치고 나머지 phase는 main에 바로 반영돼요. **어느 쪽이든 사람의
승인 없이 푸시하지 않아요.**

PR 본문에 이 네 가지를 담아요.

- **무엇을** — 바뀐 값과 파일
- **왜** — 어떤 문제를 해결하거나 무엇을 가능하게 하는지
- **영향 범위** — 어느 phase, 어느 클러스터, 재시작이 필요한지
- **되돌리는 법** — revert로 충분한지, 아니면 추가 조치가 필요한지

한 PR에 한 목적만 담아요. 조사하다 발견한 다른 문제는 별도 PR로 분리하고 본문에
언급만 해요.

## 반영 확인

머지가 곧 반영은 아니에요. Argo가 sync한 뒤에 확인해요.

1. 저장소에 머지됐는가
2. Argo가 그 revision을 sync했는가
3. 클러스터에서 실제로 바뀌었는가 (kubernetes 도구로 관측)
4. 의도한 효과가 났는가 (grafana로 지표 확인)

3까지만 보고 "적용됐다"고 말하지 않아요. 뜬 것과 동작하는 것은 다른 얘기예요.

## 되돌릴 때

- 되돌리는 것도 커밋이에요. 클러스터에서 손으로 원복하면 다음 sync에 다시 돌아와요.
- `prune: true`라서 저장소에서 리소스를 지우면 클러스터에서도 사라져요. 파일을
  지우는 변경은 영향 범위를 먼저 밝히고 확인을 받아요.
- 급한 완화가 필요하면 무엇을 임시로 했는지 명시하고, 저장소에 반영할 후속 작업을
  같이 남겨요.
