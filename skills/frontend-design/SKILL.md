---
name: frontend-design
description: >
  웹 페이지, 랜딩 페이지, 화면 레이아웃이나 React·Vue 컴포넌트를 설계하고 완성된
  코드를 작성할 때 로드해요. 목적에 맞는 시각 방향과 반응형·접근성을 함께 다뤄요.
  화면 안에 들어갈 차트의 데이터 표현은 tufte-charts가 맡아요.
---

# 프론트엔드 디자인

사용 목적이 분명하고 실제로 동작하는 UI 코드와 디자인 결정을 제공해요.

## 디자인 방향

코드를 작성하기 전에 다음을 정해요.

- 사용자가 이 화면에서 완료해야 하는 핵심 행동
- 대상 사용자와 사용 환경
- 기존 브랜드·디자인 시스템·프레임워크 제약
- 화면을 기억하게 만드는 하나의 시각적 특징

사용자가 스타일을 지정하지 않았다면 목적에 맞는 방향 하나를 선택하고 짧게 밝혀요.
여러 스타일을 섞지 말고 타이포그래피, 색, 간격, 모션을 같은 방향으로 맞춰요.

## 구현 규칙

- 사용자가 지정한 프레임워크와 기존 디자인 시스템을 우선해요.
- 프로젝트 맥락이 없으면 의존성 없는 HTML/CSS/JavaScript를 기본으로 해요.
- CSS 변수로 색, 간격, radius와 그림자를 일관되게 관리해요.
- 모바일부터 넓은 화면까지 자연스럽게 재배치되도록 만들어요.
- hover뿐 아니라 focus, active, disabled, loading, empty, error 상태를 고려해요.
- 애니메이션은 정보의 변화나 위계를 설명할 때만 사용하고
  `prefers-reduced-motion`을 지원해요.
- 아이콘은 의미를 보조하게 사용하고 장식용 이모지로 대체하지 않아요.
- 외부 폰트나 라이브러리는 사용 이유를 밝히고, 불필요하면 추가하지 않아요.

## 접근성

- 의미에 맞는 HTML 요소와 올바른 heading 순서를 사용해요.
- 키보드만으로 모든 동작을 수행할 수 있게 해요.
- form control에는 보이는 label을 연결해요.
- 상태를 색만으로 구분하지 않아요.
- 텍스트와 컨트롤의 대비를 충분히 확보해요.
- 필요한 경우에만 ARIA를 사용하고 native semantics를 우선해요.

## 시각 품질

- 목적 없는 보라색 gradient, 과도한 glassmorphism, 동일한 카드 반복을 기본값으로 쓰지 않아요.
- 본문 가독성을 희생하는 display font나 지나친 장식을 피하고, 강조 지점을 제한해요.
- 모든 요소에 border와 shadow를 겹쳐 쓰지 않아요.
- 데이터가 적으면 빈 공간을 억지 카드로 채우지 않아요.
- 실제 길이의 제목, 오류 메시지와 긴 콘텐츠에서도 레이아웃이 유지되게 해요.

## 산출물

사용자가 바로 복사해 실행할 수 있는 완성된 코드를 제공해요. 여러 파일이 필요하면
파일명을 heading으로 구분해요. 실제 파일을 생성하거나 실행하지 못한 환경에서는
생성·검증했다고 말하지 않아요.

코드 뒤에는 다음만 짧게 덧붙여요.

- 선택한 디자인 방향
- 실행에 필요한 의존성이나 전제
- 직접 확인하지 못한 브라우저·프레임워크 제약
